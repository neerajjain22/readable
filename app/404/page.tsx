import type { Metadata } from "next"
import NotFound from "../not-found"

export const metadata: Metadata = {
  title: "Page Not Found | Readable",
  description: "The page you requested could not be found.",
}

export default NotFound
